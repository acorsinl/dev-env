# Vagrant specific configuration
default ansible_ssh_host=127.0.0.1 ansible_ssh_port=2222 ansible_ssh_keyfile=~/.ssh/id_rsa

# Roles and servers
[webservers]
default

[microservices]
default

[mysqlservers]
default

[mongoservers]
default

[lbservers]
default
